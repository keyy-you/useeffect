import React from "react";
import ReactDOM from "react-dom";
import BelajarUseEffect from "./useEffect";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <BelajarUseEffect />
  </React.StrictMode>,
  rootElement
);
